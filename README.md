# PDP-11 CPU & Peripheral Emulator v1.0

Высокоточный эмулятор 16-битного микропроцессора архитектуры PDP-11 (LSI-11), написанный на языке C. Проект включает в себя полноценное ядро CPU, кастомный движок автоматического юнит-тестирования и эмуляцию периферийных устройств ввода-вывода (Memory-Mapped I/O).

## 🚀 Основные возможности эмулятора

*   **Полная эмуляция ОЗУ (64 Кб)**: Реализован побайтовый (`b_read`/`b_write`) и пословный (`w_read`/`w_write`) доступ к памяти с проверкой четности адресов и границ ОЗУ.
*   **Все 8 канонических мод адресации**: От регистровой (Мода 0) до индексной косвенной со знаковым расширением смещений (Мода 7).
*   **Железные правила PC и SP**: Аппаратная защита указателя стека `SP` (R6) и счетчика команд `PC` (R7) — изменение адреса для них всегда строго равно 2 байтам, даже в байтовых инструкциях.
*   **Изолированная периферия (MMIO)**: Реализована аппаратная изоляция портов дисплея. Регистр состояния `OSTAT` (0177564) всегда рапортует о готовности устройства (`0200`), а запись в регистр данных `ODATA` (0177566) перехватывается и выводится на экран терминала.
*   **Двухрежимный логгер**: Централизованная система `print_log` с поддержкой уровней логирования от детальной трассировки (`LOG_TRACE`) до полного отключения логов процессора (`LOG_OFF`).

## 🛠️ Поддерживаемый набор инструкций

*   **Управление процессором**: `HALT`
*   **Двухадресные**: `MOV`, `MOVB`, `ADD`
*   **Одноадресные**: `CLR`, `TSTB`
*   **Ветвления и циклы**: `SOB`, `BR`, `BNE`, `BEQ`, `BPL`

## 🧪 Тестовый фреймворк

В проект встроен автономный движок автоматического тестирования, покрывающий **29 изолированных тест-кейсов**:
1.  Валидация физики чтения/записи ОЗУ.
2.  Корректность декодирования опкодов и масок.
3.  Проверка всех 8 режимов вычисления эффективных адресов (`get_mr`).
4.  Математика выставления флагов условий PSW (`NZVC`), включая перенос знака `C` при 32-битном сложении и выявление знакового переполнения `V`.

## 📦 Сборка и запуск проекта

Для сборки проекта вам понадобятся компилятор `gcc` и утилита `make`.

### 1. Компиляция эмулятора
```bash
make
```

### 2. Запуск автоматических тестов (Все 29 тестов)
```bash
./build/pdp11 --testall
```

### 3. Запуск одиночного теста по ID или имени
```bash
./build/pdp11 --test 29
# или
./build/pdp11 --test test_tstb
```

### 4. Запуск бинарного файла программы в режиме тишины (LOG-OFF)
```bash
./build/pdp11 test/08_hello/08_hello.pdp.o -logoff
```

### 5. Запуск бинарного файла в режиме пошаговой трассировки Macro-11
```bash
./build/pdp11 test/08_hello/08_hello.pdp.o
```

## 📂 Структура исходного кода

*   `src/config.h` — Конфигурация размеров памяти (`MEMSIZE`) и базовых типов данных.
*   `src/cpu.h` / `src/cpu.c` — Декодер инструкций, логика адресации и выполнение команд CPU.
*   `src/memory.h` / `src/memory.c` — Оперативная память, изоляция портов ввода-вывода дисплея и загрузчик бинарных файлов.
*   `src/logger.h` / `src/logger.c` — Многоуровневый системный логгер.
*   `src/tests.h` / `src/tests.c` — Таблица и реализация 29 автоматических юнит-тестов.
*   `src/main.c` — Главная точка входа и разбор параметров командной строки.

## 🤝 Благодарности

Выражаю глубокую благодарность **Татьяне Николаевне Дербышевой** (МФТИ, кафедра информатики и вычислительной математики) за потрясающий и глубокий курс по архитектуре ЭВМ на платформе Stepik: [Разработка эмулятора архитектуры PDP-11](https://stepik.org/65259), материалы которого легли в основу физики и логики данного эмулятора.

---
**Разработчик:** Роман Кроль
